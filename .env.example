# Vercel AI Gateway (not required on a linked Vercel deployment using OIDC)
AI_GATEWAY_API_KEY=

# Private inbound HTTP access for the Mac bridge or another trusted client
BURN_MODE_USERNAME=christopher
BURN_MODE_PASSWORD=

# Optional Apple/iMessage bridge. Omit both to run conversationally without it.
BURN_MODE_BRIDGE_URL=http://127.0.0.1:8787
BURN_MODE_BRIDGE_TOKEN=
